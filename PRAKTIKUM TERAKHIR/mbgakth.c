#include <stdio.h>
#include <string.h>

int main(){
    int N, M;
    scanf("%d %d", &N, &M);
    
    for (int i = 0; i < N; i++)
    {
        for (int j = 0; i < M; i++)
        {
            printf("%d %d", i, j);
        }
        
    }
    





}